
const enum FilterType {
	ALL = 'everything',
	FUTURE = 'future',
	PRESENT = 'present',
	PAST = 'past'
}

const enum SortType {
	DAY = 'sort-day',
	TIME = 'sort-time',
	PRICE = 'sort-price'
}
export {FilterType, SortType};
